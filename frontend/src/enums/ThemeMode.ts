/**
 * @description Enum for theme mode
 * @readonly
 * @enum {string}
 * @example
 * ThemeMode.LIGHT // "light"
 * ThemeMode.DARK // "dark"
 */
enum ThemeMode {
  LIGHT = "light",
  DARK = "dark",
  SYSTEM = "system",
}

export default ThemeMode;
